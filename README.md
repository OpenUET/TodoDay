# 🔅 TodoDay 🔅

```shell
# Run app
yarn dev:install
yarn dev
```
Or:
```shell
# Run ReactJS client:
cd client
yarn install
yarn dev
```
```shell
# Run NodeJS server:
cd server
yarn install
yarn dev
```